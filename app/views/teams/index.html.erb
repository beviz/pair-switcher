<%= form_for Team.new, html: { style: 'max-width: 400px' } do |f| %>
  <div class="form-group">
    <label>Team Name:</label>
    <%= f.text_field :name, class: 'form-control', required: true %>
  </div>
  <%= submit_tag 'Create your team', class: 'btn btn-primary' %>
<% end %>
